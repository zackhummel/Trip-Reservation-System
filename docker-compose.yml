version: '3.10'
services:
  web:
    build: 
      context: .
      dockerfile: Dockerfile
    image: trip-reservation_system_image_flask
    container_name: trip-reservation_system_flask
    ports:
      - "5005:5000"
    volumes:
      - .:/app
    environment:
      FLASK_DEBUG: "1"
      
